import { Activity, BriefcaseBusiness, Cpu, Factory, MapPinned, Search } from 'lucide-react';
const items = [{key:'google_search',name:'Search activity',icon:Search},{key:'google_jobs',name:'Hiring signals',icon:BriefcaseBusiness},{key:'google_news',name:'News activity',icon:Activity},{key:'google_scholar',name:'Research activity',icon:Cpu},{key:'google_shopping',name:'Product activity',icon:Factory},{key:'google_maps',name:'Supplier discovery',icon:MapPinned}];
export default function MarketPulse({ data, mock }) { return <section className="section"><div className="section-heading"><div><div className="eyebrow">Signal coverage</div><h2>Market pulse</h2></div><span className="source-caption">{mock ? 'Development sample' : 'Observed results by source'}</span></div><div className="pulse-grid">{items.map(({key,name,icon:Icon})=>{const results=data?.[key]?.results; const count=Array.isArray(results)?results.length:0; return <article className="pulse-card" key={key}><div className="pulse-top"><span className="icon-box"><Icon size={17}/></span><span className="live-dot"/></div><div className="pulse-count">{count}</div><div className="pulse-label">{name}</div><div className="pulse-foot">{count ? 'results collected' : 'No results returned'}</div></article>})}</div><p className="disclaimer">Counts reflect returned records. These are coverage measures, not market scores or validated demand estimates.</p></section>; }
