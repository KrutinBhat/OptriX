import { LineChart } from 'lucide-react';
export default function MarketTrend({ mock }) { return <section className="panel trend-panel"><div className="section-heading"><div><div className="eyebrow">Activity over time</div><h2>Market trend</h2></div><span className="tag muted">{mock ? 'Illustrative' : 'Not provided by API'}</span></div><div className="trend-empty"><div className="chart-icon"><LineChart size={22}/></div><strong>Time-series data isn’t available yet</strong><span>The current API returns collected records without historical activity. Trend lines will appear when the backend exposes dated signal data.</span></div></section>; }
