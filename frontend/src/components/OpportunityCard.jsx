import { ArrowUpRight, ChevronRight } from 'lucide-react';
export default function OpportunityCard({ opportunity, onExplore, mock }) { return <article className="opportunity-card"><div className="opp-card-top"><span className="tag">Research hypothesis</span><span className="opp-arrow"><ArrowUpRight size={17}/></span></div><h3>{opportunity.title}</h3><p>{opportunity.description}</p><div className="indicator-row">{opportunity.indicators.map(([name,value])=><div className="indicator" key={name}><span>{name}</span><b>{value}</b></div>)}</div><div className="evidence-counts"><span><i className="support-dot"/> {opportunity.supporting.length} supporting points</span><span><i className="counter-dot"/> {opportunity.counter.length} counterpoints</span></div><button className="text-button" onClick={onExplore}>Explore opportunity <ChevronRight size={16}/></button>{mock&&<div className="micro-note">Development example · not a live backend opportunity</div>}</article>; }
